% ─────────────────────────────────────────────────────────────────────────────
% Fig 1 — CRSC Evaluation Framework
% Layout: Variant A — central spine, top-to-bottom, grayscale IEEE
% ─────────────────────────────────────────────────────────────────────────────

\begin{figure}[t]
\centering
\resizebox{0.96\columnwidth}{!}{%
\begin{tikzpicture}[
  font=\small\sffamily,
  % ── Node base ────────────────────────────────────────────────────────────
  box/.style = {
    draw=black!78, rounded corners=3pt, align=center,
    inner sep=5pt, line width=0.75pt
  },
  % ── Stage nodes (central spine) ──────────────────────────────────────────
  spine/.style = {box, minimum width=3.6cm, minimum height=1.0cm},
  nllm/.style  = {spine, fill=gray!10},
  nadv/.style  = {spine, fill=gray!30, line width=1.0pt},
  nft/.style   = {spine, fill=gray!10},
  nprobe/.style= {spine, fill=gray!6,  draw=black!55, dashed},
  neng/.style  = {spine, fill=gray!18, line width=1.1pt},
  % ── Metric nodes (bifurcation) ────────────────────────────────────────────
  nmet/.style  = {box, minimum width=2.7cm, minimum height=1.0cm,
                  fill=white, draw=black!60, dashed},
  % ── CRSC Score ────────────────────────────────────────────────────────────
  nscr/.style  = {box, minimum width=3.8cm, minimum height=1.25cm,
                  fill=gray!20, line width=1.35pt},
  % ── Agent nodes (trifurcation) ────────────────────────────────────────────
  nag/.style   = {box, minimum width=2.5cm, minimum height=1.05cm,
                  fill=gray!10},
  % ── Risk report ───────────────────────────────────────────────────────────
  nrsk/.style  = {box, minimum width=5.6cm, minimum height=1.0cm,
                  fill=gray!25, line width=1.0pt},
  % ── Arrows ───────────────────────────────────────────────────────────────
  amain/.style = {-{Stealth[length=5pt,width=3.5pt]}, line width=0.95pt},
  asec/.style  = {-{Stealth[length=4pt,width=3pt]},   line width=0.75pt},
  adash/.style = {-{Stealth[length=4pt,width=3pt]}, dashed,
                  line width=0.7pt, black!65},
  % ── Edge labels ──────────────────────────────────────────────────────────
  el/.style    = {font=\scriptsize\sffamily, fill=white,
                  inner sep=1.5pt, pos=0.5},
]

% ══ CENTRAL SPINE ════════════════════════════════════════════════════════════

\node[nllm] (llm) at (0,0) {%
  \textbf{Pre-trained LLM}\\[-1pt]
  {\scriptsize $\theta_{\text{base}}$\quad
   $N\in\{7\text{B},\,13\text{B},\,70\text{B}\}$}};

\node[nadv] (inj) at (0,-1.78) {%
  \textbf{Backdoor Injection}\\[-1pt]
  {\scriptsize trigger $\gamma$\enspace rate
   $\rho\!\in\!\{0.001,\,0.005,\,0.01,\,0.05\}$}};

\node[nft]  (ft)  at (0,-3.56) {%
  \textbf{Post-training}\\[-1pt]
  {\scriptsize SFT\,/\,RLHF\quad
   $\theta_{\text{base}}\!\to\!\theta_{\text{ft}}$}};

\node[nprobe] (probe) at (0,-5.34) {%
  \textbf{Probe Set}\;$\mathcal{T}$\\[-1pt]
  {\scriptsize $K\!=\!500$\quad seed\,42\quad
   50\,\% benign\,+\,50\,\% triggered}};

\node[neng] (eng) at (0,-7.12) {%
  \textbf{CRSC Engine}\\[-1pt]
  {\scriptsize extract
   $h_{\text{base}},\,h_{\text{ft}},\,\ell_{\text{base}},\,\ell_{\text{ft}}$}};

% ══ BIFURCATION ══════════════════════════════════════════════════════════════

\node[nmet] (dhid) at (-2.3,-9.05) {%
  $\boldsymbol{\Delta_{\!\text{hidden}}}$\\[-2pt]
  {\scriptsize
   $\dfrac{\|h_{\text{base}}-h_{\text{ft}}\|_F}{\|h_{\text{base}}\|_F}$}};

\node[nmet] (dh)   at ( 2.3,-9.05) {%
  $\boldsymbol{\Delta H}$\\[-2pt]
  {\scriptsize
   $H(\ell_{\text{ft}})-H(\ell_{\text{base}})$}};

% ══ CONVERGENCE → CRSC SCORE ═════════════════════════════════════════════════

\node[nscr] (score) at (0,-11.20) {%
  \textbf{CRSC Score}\\[2pt]
  {\small$\alpha\,(1\!-\!\Delta_{\text{hid}})\;+\;\beta\,\Phi(-\Delta H)$}\\[-2pt]
  {\scriptsize$\alpha\!=\!\beta\!=\!0.5$\quad
   threshold\;$\tau\!=\!0.62$}};

% ══ TRIFURCATION → AGENTS ════════════════════════════════════════════════════

\node[nag] (ag1) at (-3.15,-13.25) {%
  \textbf{Agent 1}\\[-1pt]
  {\scriptsize\textit{Detection}}\\[-2pt]
  {\scriptsize F1\,=\,0.891\quad AUC\,=\,0.924}};

\node[nag] (ag2) at (0,-13.25) {%
  \textbf{Agent 2}\\[-1pt]
  {\scriptsize\textit{Analysis}}\\[-2pt]
  {\scriptsize $r\!=\!0.847$\quad $p\!<\!0.001$}};

\node[nag] (ag3) at ( 3.15,-13.25) {%
  \textbf{Agent 3}\\[-1pt]
  {\scriptsize\textit{Response}}\\[-2pt]
  {\scriptsize Prec@$\tau\!=\!0.903$}};

% ══ CONVERGENCE → RISK REPORT ════════════════════════════════════════════════

\node[nrsk] (risk) at (0,-15.35) {%
  \textbf{Risk Report}\\[-1pt]
  {\scriptsize LOW\,/\,MEDIUM\,/\,HIGH\quad
   MITRE ATLAS\,·\,NIST AI RMF}};

% ══ SPINE ARROWS ═════════════════════════════════════════════════════════════

\draw[amain] (llm.south)   -- node[el,right]{inject $\gamma,\rho$}
             (inj.north);
\draw[amain] (inj.south)   -- node[el,right]{poison $\theta_{\text{base}}$}
             (ft.north);
\draw[amain] (ft.south)    -- node[el,right]{$\theta_{\text{ft}}$ deployed}
             (probe.north);
\draw[amain] (probe.south) -- node[el,right]{$h_{\text{base}},h_{\text{ft}}$}
             (eng.north);

% ══ BIFURCATION (engine → metrics) ══════════════════════════════════════════

% Junction point below engine
\coordinate (jA) at (0,-7.72);
\draw[asec] (eng.south) -- (jA);
\draw[asec] (jA) -| (dhid.north);
\draw[asec] (jA) -| (dh.north);
\node[el, anchor=north, yshift=-2pt] at (jA) {\scriptsize extract};

% ══ CONVERGENCE (metrics → score) ════════════════════════════════════════════

\coordinate (jB) at (0,-10.52);
\draw[asec] (dhid.south) |- (jB);
\draw[asec] (dh.south)   |- (jB);
\draw[asec] (jB) -- (score.north);

% ══ TRIFURCATION (score → agents) ════════════════════════════════════════════

\coordinate (jC) at (0,-12.0);
\draw[asec]  (score.south) -- (jC);
\draw[asec]  (jC) -| (ag1.north);
\draw[amain] (jC) -- (ag2.north);
\draw[adash] (jC) -| (ag3.north);
\node[el, anchor=north, yshift=-2pt] at (jC)
  {\scriptsize CRSC$\,\geq\!\tau$?};

% ══ CONVERGENCE (agents → risk) ══════════════════════════════════════════════

\coordinate (jD) at (0,-14.5);
\draw[asec]  (ag1.south) |- (jD);
\draw[amain] (ag2.south) -- (jD);
\draw[adash] (ag3.south) |- (jD);
\draw[amain] (jD) -- (risk.north);

% ══ BOUNDARY BOXES ═══════════════════════════════════════════════════════════

\begin{scope}[on background layer]
  % Adversarial surface
  \node[
    draw=black!55, dashed, rounded corners=4pt, line width=0.65pt,
    fill=gray!5, inner sep=7pt,
    fit=(inj),
  ] (badv) {};
  \node[font=\scriptsize\sffamily, above=2pt of badv.north,
        text=black!60] {Adversarial input};

  % Metric layer
  \node[
    draw=black!50, dotted, rounded corners=4pt, line width=0.7pt,
    fill=gray!3, inner sep=9pt,
    fit=(dhid)(dh)(score),
  ] (bmet) {};
  \node[font=\scriptsize\sffamily, above=2pt of bmet.north,
        text=black!60] {CRSC metric layer};

  % Agent evaluation layer
  \node[
    draw=black!50, dashed, rounded corners=4pt, line width=0.65pt,
    fill=gray!3, inner sep=7pt,
    fit=(ag1)(ag2)(ag3),
  ] (bag) {};
  \node[font=\scriptsize\sffamily, above=2pt of bag.north,
        text=black!60] {Three-agent evaluation};
\end{scope}

\end{tikzpicture}%
}% end resizebox
\caption{CRSC evaluation framework (central-spine layout).
Starting from a pre-trained LLM, an adversary injects a backdoor trigger $\gamma$
at poisoning rate $\rho$; the model is post-trained via SFT or RLHF
($\theta_{\text{base}}\!\to\!\theta_{\text{ft}}$).
The CRSC Engine applies a fixed probe set $\mathcal{T}$ ($K\!=\!500$, seed\,42)
and extracts hidden states and loss sequences from both checkpoints.
The pipeline bifurcates to compute $\Delta_{\text{hidden}}$ (Frobenius drift)
and $\Delta H$ (entropy shift), which converge into the CRSC score
($\alpha\!=\!\beta\!=\!0.5$, $\tau\!=\!0.62$).
Three specialized agents perform detection (Agent~1: F1\,=\,0.891),
analysis (Agent~2: $r\!=\!0.847$), and response (Agent~3: Prec@$\tau\!=\!0.903$),
whose outputs are consolidated into a structured risk report.}
\label{fig:overview}
\end{figure}
